import styled from "styled-components";

const SectionStyled = styled.div`
  text-align: left;
  & .btn-new-item {
    margin-left: 20px;
    text-align: left;
    margin-top: 15px;
  }
`;

export default SectionStyled;
