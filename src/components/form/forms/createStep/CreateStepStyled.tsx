import styled from "styled-components";

const CreateStep = styled.div`
& .button {
    color: #008CBA;
}

& .needs-validation {
    font-family: 'Quicksand', sans-serif;
}
`;

export default CreateStep;
